The build folder is purposely empty and gives a standard of where to start the build from. 

To build and run from Terminal:

1. Navigate to the ~/build folder. (optionally clean this folder with "rm -rf *")
2. run "cmake .."
3. run "make"
4. run the generated program in the ~/build/bin directory. 
